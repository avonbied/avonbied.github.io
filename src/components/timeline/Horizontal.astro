---

---

<style lang="scss">
	.point {
		background-image: radial-gradient(circle, #000 2%, transparent 2%);
		.infoCard {
			display: flex;
			flex-wrap: wrap;
			flex: 0 0 50%;
			padding: 24px;
			min-height: 150px;
		}
		.right {
			margin-left: auto;
			border-left: 2px solid #fff;
		}
		.left {
			margin-right: auto;
			border-right: 2px solid #fff;
			flex-direction: row-reverse;
		}
	}
</style>