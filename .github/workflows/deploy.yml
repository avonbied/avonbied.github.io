name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  deploy-nextjs-gh-pages:
    uses: avonbied/avonbied/.github/workflows/nextjs-gh-pages.yml@main
